#include <bits/stdc++.h>
using namespace std;
#define pi 3.14
//if not defined then define
#ifndef pi
    #define pi 3.1425
#endif
//#define max(x,y) x>y?x:y //error
#define max(x,y) (x>y?x:y)
#define msg(x) #x
int main() {
    cout<<pi;
    cout<<"\nmax value is "<<max(7,8);
    cout<<endl<<msg(dhana);
    return 0;
}